{
  "_comment": "Standalone install, for people who do not want the plugin. Copy output-styles/claudesplain.md to ~/.claude/output-styles/ and scripts/rewrite.py somewhere executable, then merge this into your settings.json.",
  "outputStyle": "claudesplain",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PROJECT_DIR}/.claude/scripts/rewrite.py",
            "timeout": 30,
            "statusMessage": "checking phrasing"
          }
        ]
      }
    ]
  }
}
